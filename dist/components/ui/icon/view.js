'use strict';

Component({
    properties: {
        name: String
    },

    externalClasses: ['c-class'],
    methods: {
        handleTap: function handleTap() {
            // this.triggerEvent('tap');
        }
    }
});