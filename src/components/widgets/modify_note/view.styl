@import "../../../assets/css/define.styl";

$prefix = c-modify-note;

.{$prefix}{
    textarea{
        width: 100%;
        height: 140rpx;
        $border('');
        text-align: left;
        box-sizing: border-box;
        padding: 32rpx;
    }
}
